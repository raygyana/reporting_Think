import { PHPGlobalSettings, MITGlobalSettings, UCPGlobalSettings } from './think-global-setting';

export class GlobalSettings {

      static PHP_UI_SETTING = PHPGlobalSettings.PHP_UI_SETTING;

      static UCP_UI_SETTING = UCPGlobalSettings.UCP_UI_SETTING;

      static MIT_UI_SETTING = MITGlobalSettings.MIT_UI_SETTING;
}
